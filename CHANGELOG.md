## 0.0.1

* Initial release with Line and Solid vector icon widgets.
* Replaced icon font approach with compiled `.svg.vec` assets for higher visual fidelity.
* Public API: `PlumpIcon`, `PlumpIconData`, and `PlumpIcons.*Line` / `PlumpIcons.*Solid`.
